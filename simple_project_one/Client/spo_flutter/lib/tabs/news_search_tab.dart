import 'package:flutter/material.dart';

class NewsSearchTab extends StatelessWidget {
  NewsSearchTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('no news'));
  }
}
